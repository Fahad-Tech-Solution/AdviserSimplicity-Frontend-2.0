import React, { useEffect } from 'react'

const NewLoadingExclusion = (props) => {
    useEffect(() => {
        console.log(props)
    }, [])

    return (
        <div>

        </div>
    )
}

export default NewLoadingExclusion
